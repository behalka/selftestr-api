const sessions = require('./sessions')
const users = require('./users')
const testModels = require('./testModels')
const questionModels = require('./questionModels')
const testInstances = require('./testInstances')

module.exports = {
  questionModels,
  sessions,
  testInstances,
  testModels,
  users,
}
